# TraceOS

**Evidence-driven agentic investigation infrastructure.**

TraceOS answers one question: *given all this conflicting evidence, what actually happened?* It reconstructs timelines, cross-checks evidence sources against each other, actively tries to disprove its own conclusion, and verifies the result before handing it to a human reviewer.

**First application:** payment and order dispute investigation.

---

## Table of contents

- [Who has this problem?](#who-has-this-problem)
- [What bottleneck makes it worth solving?](#what-bottleneck-makes-it-worth-solving)
- [Why plain LLM review isn't enough](#why-plain-llm-review-isnt-enough)
- [Improvement Changelog](#improvement-changelog)
- [Evaluation results](#evaluation-results)
- [Main failure mode](#main-failure-mode)
- [Hot take](#hot-take)
- [What we deliberately cut and why](#what-we-deliberately-cut-and-why)
- [Full vision](#full-vision)
- [MVP scope](#mvp-scope-hackathon-build)
- [Architecture](#architecture)
- [Ground truth schema](#ground-truth-schema)
- [Investigation output & the HTML report](#investigation-output--the-html-report)
- [Running the MVP](#running-the-mvp)
- [Provider routing](#provider-routing)
- [Deployment (web service)](#deployment-web-service)
- [Case set plan](#case-set-plan-1012-total)
- [Human checkpoint](#human-checkpoint)
- [V2 backlog](#v2-backlog)

---

## Who has this problem?

<!-- TODO: 2-3 sentences naming the real user. e.g. "Support and fraud analysts
at a payments/fintech company who handle dispute tickets - a customer claims
a payment succeeded, internal records disagree, and the analyst has to
manually reconcile provider records, bank settlement, order records, and the
customer's own claim/receipt before deciding whether to honor or reject the
dispute." -->

## What bottleneck makes it worth solving?

<!-- TODO: Why is this expensive/slow/error-prone today? e.g. "Each dispute
requires cross-referencing 4-5 separate systems by hand. Evidence sources
disagree with each other more often than they agree cleanly, and a rushed
analyst under ticket-volume pressure can miss a contradiction (e.g. a
customer's receipt shows 'successful' while the provider record shows
'failed' for the same reference) and side with the wrong party." -->

## Why plain LLM review isn't enough

A single prompt with all the evidence dumped in can only pattern-match against what looks plausible. It has no mechanism to *check* a hypothesis, no adversarial step trying to disprove its own first answer, and nothing stopping it from treating a customer's claim as equivalent to a system-of-record entry. TraceOS's agent pipeline exists specifically to close those three gaps — see the architecture and changelog below for how.

---

## Improvement Changelog

*Replace `__` with your real `npm run eval` numbers once the run completes.*

| Stage | What we tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| Baseline | Single prompt, all evidence dumped in, forced into the same structured output schema as the agent | Accuracy: __% on 12 cases | Established the starting point |
| Iteration 1 | Investigator agent alone (no contradiction check, no verifier) | Accuracy: __% (+__ vs baseline) | [Kept/Removed] — explain what changed |
| Iteration 2 | Added Contradiction agent — adversarial pass that actively tries to disprove the investigator's conclusion | Accuracy / false-positive rate: __% | Kept — explain what it caught that the investigator alone missed |
| Iteration 3 | Added Verifier agent with reject → single retry | Rejection rate: __%, corrected-conclusion accuracy after retry: __% | Kept — this is the biggest single driver of trustworthiness; explain a specific case it caught |
| Iteration 4 | Multi-provider router (Groq → OpenRouter) with circuit breaker | Reliability: __% of calls succeeded without manual intervention across the full eval run | Kept — explain the tradeoff vs a single-provider setup |
| Final | Combined: investigator → contradiction → verifier (+1 retry), multi-provider router | Accuracy: __%, false positives: __% | Main contribution: [state it] |

## Evaluation results

Primary metric: does the structured verdict name the right customer, correctly flag claim validity, and surface the right contradicting transaction reference (see [Ground truth schema](#ground-truth-schema)) — scored automatically, no manual grading, no LLM-judging-LLM step.

| Metric | Baseline | Agent | Change |
|---|---|---|---|
| Accuracy (correct verdict) | __% | __% | +__pp |
| False positive rate | __% | __% | -__pp |
| Verifier rejection rate | n/a | __% | — |
| Avg time / case | __s | __s | +__s |
| Cost / case | $__ | $__ | +$__ |

12 cases total (see [Case set plan](#case-set-plan-1012-total)), including at least one genuinely ambiguous case — see that case's `ground_truth.json` for why it's hard and how each system handled it.

Full per-case results: `results.json` (generated by `npm run eval`), including `providerStats` (attempts/successes per provider, fallback events, circuit-breaker trips).

## Main failure mode

<!-- TODO: What does the agent still get wrong? Cite a specific case_id and
what happened, not a generality. If the challenging case is where it broke,
say so explicitly - that's exactly what the rubric wants to see. -->

## Hot take

<!-- TODO: One practical lesson earned from an observed failure. e.g. "A
verifier that can reject but isn't forced to check specific fields (customer
identity, transaction reference, claim validity) individually tends to
rubber-stamp confident-sounding investigator output - it caught contradiction
errors far more often than it caught unsupported-claim errors, which tells us
where to invest next: structured per-field verification, not just a
free-text 'approved: true/false'." -->

## What we deliberately cut and why

- **More than one verifier-triggered retry** — an open-ended reject/retry loop is a time sink with diminishing returns for a 24h build, not a reward. One retry with explicit feedback was enough to demonstrate the mechanism.
- **A Timeline Agent** — sorting evidence by timestamp is a one-line deterministic operation once evidence carries a timestamp field. An LLM call here would add latency and a failure point for zero benefit.
- **An Evidence Agent over structured sources** — CSV parsing is just parsing. The one LLM call in evidence handling is reserved for *unstructured* sources (chat, receipt text) that actually need normalization into the shared schema.
- **Postgres / persistence, Next.js dashboard, evidence graph visualization, KYC domain pack** — see [V2 backlog](#v2-backlog). All genuinely useful, none of them change whether the core investigation mechanism works, which is what a 24-hour build should prove first.

---

## Full vision

Where this goes beyond the hackathon:

- **Multi-source evidence ingestion** — CSV and text today; PDFs, images, and screenshots later
- **Evidence graph** — entity relationships between evidence items, not just a flat list
- **Multi-agent pipeline** — extraction → timeline → investigation → adversarial challenge → verification → retry-on-reject
- **Persistent case storage** — Postgres, case history, full audit trail
- **Analyst dashboard** — Next.js UI with an approve / reject / escalate review queue
- **Multiple domain packs** on one shared investigation engine:
  - Payments/order disputes (v1, this build)
  - KYC / onboarding consistency audits (v2)
  - Chargebacks, refund fraud, internal ops discrepancies (later)

None of the Postgres/Next.js/dashboard layer is a build target right now — it's context so the MVP doesn't get architected into a corner it can't grow out of.

---

## MVP scope (hackathon build)

**Definition of done:** a CLI tool that takes a case folder, runs baseline vs. agent pipeline on it, outputs a structured verdict plus a human-readable report, logs full trajectories, and scores itself against ground truth across 12 cases with real numbers in a comparison table.

| Component | MVP version |
|---|---|
| Evidence handling | Deterministic CSV parse + one lightweight LLM call to normalize *unstructured* sources (chat, receipt text) into the same schema. |
| Timeline | Deterministic sort by timestamp in code. No Timeline Agent. |
| Investigator | LLM agent, structured output (`InvestigationSchema`) |
| Contradiction / adversarial review | LLM agent, structured output, explicitly instructed to try to disprove the investigator's conclusion |
| Verifier | LLM agent, can reject and return `approved: false`, which triggers exactly **one** retry of the investigator with the verifier's feedback appended. |
| Report renderer | Boxed ASCII dossier (terminal) plus a self-contained HTML dossier per case (`report/<case-id>.html`) via `src/report/render-html.ts` — verified/rejected stamp, confidence meters, findings, contradictions, evidence trail, timeline, unresolved questions, and a "Verifier objections" panel on rejection. |
| Baseline | Single prompt, all raw evidence dumped in, forced into the *same* output schema as the agent, so scoring is apples-to-apples |
| Eval harness | Runs both systems on all cases, scores against structured ground truth, outputs a results table |
| Trajectory logging | One JSON file per case per system: agent name → input → output → retry (if any) |
| Cases | 12 synthetic cases with structured ground truth, including at least one explicitly challenging/ambiguous case |

### Example MVP output

```
┌──────────────────────────────────────────┐
│ TRACEOS INVESTIGATION                    │
├──────────────────────────────────────────┤
│ CONCLUSION                               │
│ Customer A's payment is NOT verified.    │
│                                           │
│ CONFIDENCE                               │
│ 96%                                      │
│                                           │
│ KEY FINDING                              │
│ The successful ₦250,000 transaction      │
│ belongs to Customer B.                   │
│                                           │
│ CONTRADICTIONS                           │
│ ⚠ Receipt says TXN-773 was successful    │
│ ⚠ Provider says TXN-773 failed           │
│                                           │
│ EVIDENCE                                 │
│ ✓ payment_provider.csv                   │
│ ✓ orders.csv                             │
│ ✓ customer_chat.txt                      │
│ ✓ receipt.txt                            │
└──────────────────────────────────────────┘
```

---

## Architecture

```
traceos/
├── src/
│   ├── agents/
│   │   ├── investigator.agent.ts     # LLM, Investigation output
│   │   ├── contradiction.agent.ts    # LLM, adversarial, tries to disprove
│   │   └── verifier.agent.ts         # LLM, approve/reject + one-retry feedback
│   │
│   ├── schemas/
│   │   ├── investigation.ts          # EvidenceItem, Investigation, Verification, TimelineEvent, ...
│   │   └── trajectory.ts             # per-case/per-system run log
│   │
│   ├── lib/
│   │   ├── llm.ts                     # AI router: Groq/OpenRouter fallback + structured output
│   │   ├── provider.ts                # OpenAI-compatible transport (Groq/OpenRouter)
│   │   ├── gemini.ts                  # Gemini transport - present but NOT wired into the router (see Provider routing)
│   │   ├── gemini-retry.ts            # 429 retry with capped exponential backoff
│   │   ├── rate-limit.ts              # per-provider RPM pacing
│   │   └── prompt.ts                  # evidence serialization + timeline shaping
│   │
│   ├── evidence/
│   │   ├── extract-structured.ts     # deterministic CSV → EvidenceItem[]
│   │   ├── extract-unstructured.ts   # one LLM call: chat/receipt text → EvidenceItem[]
│   │   └── build-timeline.ts         # deterministic sort, no LLM
│   │
│   ├── workflow/
│   │   └── investigate.ts            # baseline/agent orchestration, pipeline + retry, trajectories
│   │
│   ├── report/
│   │   ├── render-html.ts            # self-contained HTML dossier (verified/rejected)
│   │   ├── render-ascii.ts           # boxed ASCII dossier (terminal)
│   │   └── bootstrap-demo.ts         # demo-only report generator (no API key needed)
│   │
│   ├── data/
│   │   └── load-case.ts
│   │
│   ├── trace.ts                      # trajectory persistence + eval cache
│   ├── cases.ts                      # safe case-dir resolution (path-traversal guard)
│   ├── server.ts                     # Express web service (deployable)
│   ├── baseline.ts                   # single-prompt, same schema as agent
│   ├── evaluate.ts                   # run + score both systems, results table
│   └── index.ts                      # CLI entry (single case)
│
├── cases/                          # 12 synthetic cases
│   ├── case-01-demo/
│   │   ├── ground_truth.json       # structured, auto-scorable
│   │   ├── orders.csv
│   │   ├── payment_provider.csv
│   │   ├── bank_settlement.csv
│   │   ├── customer_chat.txt
│   │   └── receipt.txt
│   └── case-02.../ ... case-12.../
│
├── evidence/trajectories/          # generated (case.<system>.json) — eval cache
├── report/                         # generated HTML dossiers per case/system
├── results.json                    # generated eval results
├── render.yaml                     # Render web-service blueprint
├── .env.example
├── .gitignore
├── package.json
├── tsconfig.json
├── README.md
└── REPRODUCTION.md
```

**Stack:** Node.js, TypeScript, Express, OpenAI-compatible router (Groq / OpenRouter), Zod, `csv-parse`, `dotenv`.

---

## Ground truth schema

Structured, not free text, so scoring can run unattended across baseline and agent alike.

```json
{
  "case_id": "case-01-demo",
  "correct_verified_customer": "CUST-B",
  "disputed_customer": "CUST-A",
  "disputed_customer_claim_is_valid": false,
  "key_contradiction": {
    "claimed_ref": "TXN-773",
    "actual_status": "failed",
    "real_successful_txn": "TXN-774",
    "real_successful_customer": "CUST-B"
  },
  "difficulty": "normal"
}
```

`evaluate.ts` scores by checking whether the system's structured output names the right customer, correctly flags the claim as valid/invalid, and surfaces the right contradicting transaction reference — no manual grading, no LLM-judging-LLM step.

---

## Investigation output & the HTML report

The agent pipeline returns an `Investigation` that carries **both** the fields needed for unattended scoring and the fields the report renders:

```jsonc
{
  "caseId": "case-01-demo",
  "disputedCustomer": "CUST-A",        // scoring  → ground_truth.disputed_customer
  "claimIsValid": false,               // scoring  → ground_truth.disputed_customer_claim_is_valid
  "verifiedCustomer": "CUST-B",        // scoring  → ground_truth.correct_verified_customer
  "confidence": 0.96,                  // 0..1 fraction (report shows 96%)
  "keyFinding": "The successful ₦250,000 transaction belongs to Customer B.",
  // report-only fields:
  "conclusion": "Customer A's payment claim is NOT verified.",
  "findings": ["TXN-773 was recorded as FAILED by the provider at 10:32 UTC.", "…"],
  "contradictions": ["Receipt (EVD-06) states TXN-773 succeeded; provider says it failed."],
  "supportingEvidenceIds": ["EVD-03", "EVD-04", "EVD-06"],
  "unresolvedQuestions": ["Whether the receipt was fabricated is unestablished."]
}
```

`src/report/render-html.ts` turns this into a **static, self-contained HTML dossier** (no server, no heavy JS — just a native `<details>` toggle) written to `report/<case-id>.html`:

```ts
fs.writeFileSync(`report/${caseId}.html`, renderHtmlReport({ caseId, investigation, verification, evidence, timeline }));
```

The report's verdict stamp is driven by the verifier:

- `verification.approved === true` → **VERIFIED** (green stamp), investigator's conclusion shown.
- `verification.approved === false` → **REJECTED** (red stamp), a `re-investigated after rejection` note when a retry occurred, `verification.correctedConclusion` shown in place of the investigator's conclusion, and a **"Verifier objections"** panel listing `unsupportedClaims`, `missingEvidence`, and `contradictionErrors`.

Generate the demo dossiers (one verified, one rejected) with **no API key**:

```
npm run report:demo
```

The rejected variant is the strongest visual beat for the demo video / changelog walkthrough — the stamp flips and the corrected conclusion appears.

---

## Running the MVP

Requires at least one AI provider key in `.env` — `GROQ_API_KEY` and/or `OPENROUTER_API_KEY` (see `.env.example` and [Provider routing](#provider-routing)).

```
npm run dev                    # run baseline + agent on one case (default case-01-demo)
npm run dev -- cases/case-05-contradiction
npm run eval                   # run + score both systems on all cases → results.json + comparison table
npm run eval -- --force        # bypass the trajectory cache, re-run the LLM pipeline on every case
npm run report:demo            # generate demo dossiers (verified + rejected) with NO API key
npm run typecheck
npm run build                  # compile src/ → dist/
npm run dev:server             # web service (tsx, for local dev)
npm run start:server           # web service (node dist/server.js, for deploy)
```

For one case, `npm run dev` writes `report/<caseId>.baseline.html` and `report/<caseId>.agent.html`, prints the boxed ASCII dossiers, and logs trajectories to `evidence/trajectories/<caseId>.<system>.json`.

`npm run eval`:
- loads all 12 cases,
- runs baseline + agent pipeline on each (writing HTML per case/system),
- scores each structured output against `ground_truth.json`,
- prints the comparison table and writes `results.json`.

The pipeline order is: **investigator → contradiction (adversarial) → verifier**, with exactly **one** verifier-triggered retry of the investigator before the verdict is handed to the human reviewer.

## Provider routing

The AI layer is provider-independent (`src/lib/llm.ts`). `generateStructured` tries the configured providers in order — active providers are **Groq and OpenRouter** — and on failure (429 / network / unparseable / schema-invalid output) automatically falls back to the next one:

- `AI_PROVIDER=auto` (default) uses every configured provider (Groq, OpenRouter) in priority order. Set an explicit order, e.g. `AI_PROVIDER=openrouter,groq`.
- `AI_FALLBACK=true` (default) enables the fallback; set `false` to only ever use the first configured provider.
- Model IDs are env-configurable (`GROQ_MODEL`, `OPENROUTER_MODEL`) since free-model catalogs change often.
- **A Gemini transport exists** in `src/lib/gemini.ts` but is **not currently wired into the router** — `configuredProviders()` in `llm.ts` only resolves `groq`/`openrouter`, so a `GEMINI_API_KEY` alone will not enable a working provider. This is scaffolding for a v2 addition, not an active path.

**Rate limiting** is per-provider (`src/lib/rate-limit.ts`) so a fast provider isn't throttled to the slowest one's pace:
```
RATE_INTERVAL_MS_GROQ=2000             # per-provider spacing (ms)
RATE_INTERVAL_MS_OPENROUTER=1500
```
Retries on transient 429/5xx use capped exponential backoff (`src/lib/gemini-retry.ts`, provider-agnostic despite the filename).

**Circuit breaker.** A provider that fails is tripped and skipped for a cooldown window instead of being retried repeatedly, so the router moves on fast rather than stalling on a dead provider.

**Eval caching.** The per-case trajectory files double as an eval cache: a case already present under `evidence/trajectories/` is reloaded instead of re-running the LLM pipeline, so the first benchmark consumes API quota but subsequent scoring runs do not. This is a deliberate engineering choice for reproducibility:

> The eval runner caches completed agent trajectories. The initial benchmark requires model calls; subsequent scoring runs operate entirely on cached results and do not consume API quota.

To force a full re-run, pass `--force` (`npm run eval -- --force`) or set `EVAL_USE_CACHE=false` in `.env`.

The eval harness records routing metrics — attempts, successes per provider, fallback events, and circuit-breaker trips — into `results.json` and prints them after the comparison table, so you can quantify the reliability win of fallback over a single provider.

---

## Deployment (web service)

A minimal Express wrapper (`src/server.ts`) lets judges **click a case and watch the real agent pipeline run live**, producing the same HTML dossier the CLI writes:

- `GET /` — HTML index listing all cases
- `GET /cases` — JSON list of `caseId`s
- `GET /cases/:caseId` — runs baseline + agent live and returns the HTML dossier (404 for unknown cases)

Run it locally with `npm run dev:server`, or compiled with `npm run build && npm run start:server`.

`render.yaml` is a Render blueprint (build: `npm ci && npm run build`; start: `node dist/server.js`). Connect the repo as a **Blueprint**, then set at least one provider key (`GROQ_API_KEY` / `OPENROUTER_API_KEY`) as a secret in the Render dashboard — keys are never committed (`sync: false`).

Free-tier tradeoffs to know before demo day:

- Free web services **spin down after inactivity** and take ~30–60s to wake on the next request. Hit the URL yourself a minute before recording, or use the Starter plan for the 24h window.
- Free-tier disk is **ephemeral** — runtime reports/trajectories vanish on redeploy. Regenerate them by clicking a case again; nothing permanent is stored.
- The CLI (the primary reproduction path in `REPRODUCTION.md`) keeps working alongside the server.

Because the live pipeline is rate-limited, a full case takes a while on the free tier — fine for a judge clicking at their own pace, less suited to live sync narration in the video (record the CLI run for that instead).

---

## Case set plan (10–12 total)

- **4–5 straightforward cases** — one clean contradiction each, different transaction/customer combinations
- **3–4 clean cases** — the customer's claim actually checks out, no contradiction. These measure false-positive rate.
- **2–3 harder cases** — multiple transactions, partial refunds, near-duplicate amounts across three customers, timestamp ordering that looks suspicious but isn't
- **1 genuinely ambiguous case**, even to a human, with a note in the case file explaining why. This is the rubric-required "challenging case," and doubles as the best source material for the "main failure mode" section above.

---

## Human checkpoint

Per the hackathon ground rules, TraceOS's output is a **recommendation to a fraud/support analyst**, never an automated denial or account action. No consequential action (blocking a customer, issuing a refund, closing a case) happens automatically — the HTML dossier is designed to be read and signed off on by a human reviewer before anything downstream happens.

---

## V2 backlog

- KYC / onboarding consistency auditor as a second domain pack on the same core investigation engine
- Wire the existing Gemini transport (`src/lib/gemini.ts`) into the router as a third fallback
- Evidence graph (visual, not just structured data)
- Postgres persistence + case history
- Next.js analyst dashboard with an approve/reject queue
- Multi-retry verifier loop with escalation logic
- PDF / screenshot evidence ingestion